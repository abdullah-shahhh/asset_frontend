services:
  client-panel:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:5000/api}
        VITE_MAPIFYIT_TOKEN: ${VITE_MAPIFYIT_TOKEN:-}
    restart: unless-stopped
    ports:
      - "5173:80"
