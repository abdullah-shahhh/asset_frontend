# Base URL of the backend API (includes the /api prefix, no trailing slash).
VITE_API_BASE_URL=http://localhost:5000/api

# MapifyIT tile proxy token (same basemap used across the MapifyIT product
# family). Falls back to the shared default token if unset.
VITE_MAPIFYIT_TOKEN=mfy_c7ee82a354e708f29eeb546fc36519b999db8c637d284ebf
